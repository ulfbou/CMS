@page "/"
@rendermode InteractiveServer
@using CMS.Server.Models
@using CMS.Server.Components

<h1>Drag and Drop Example</h1>

<BoxList OnBoxSelected="HandleBoxSelected" />

@code {
    private void HandleBoxSelected(ContentModel selectedBox)
    {
        // Gör något med den valda boxen, till exempel visa en meny
        Console.WriteLine($"Selected Box: {selectedBox.GetContent()}");
    }
}
